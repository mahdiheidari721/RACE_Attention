Virtual environment and quick usage

Activation:

```bash
source race/bin/activate
```

Installed packages list:

- `race/installed.txt`

Quick import test saved to:

- `race/import_test.txt`

Basic checks:

```bash
python -V
pip show torch transformers datasets
```
